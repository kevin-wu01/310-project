import { TestFolderOptions } from "./types";
import { ISuite } from "mocha";
declare function testFolder<I, O, E>(suiteName: string, target: (input: I) => O | PromiseLike<O>, folder: string, options?: Partial<TestFolderOptions<I, O, E>>): ISuite;
export { testFolder };
